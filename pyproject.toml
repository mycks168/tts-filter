[project]
name = "tts-filter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.116.1",
    "httpx>=0.28.1",
    "pyyaml>=6.0.2",
    "python-dotenv>=1.1.1",
    "pytest>=9.0.2",
    "uvicorn>=0.35.0",
]

[project.scripts]
tts-filter = "tts_filter:main"

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"
