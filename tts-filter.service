[Unit]
Description=TTS Filter API
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/tts-filter
EnvironmentFile=/opt/tts-filter/.env
ExecStart=uv run uvicorn tts_filter_api.app:app --host 0.0.0.0 --port 9191
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
